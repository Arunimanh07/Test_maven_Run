<settings>
  <mirrors>
    <mirror>
      <id>my-maven-feed</id>   <!-- MUST MATCH -->
      <url>https://pkgs.dev.azure.com/arunimanh/synk_test/_packaging/my-maven-feed/maven/v1</url>
      <mirrorOf>*</mirrorOf>
    </mirror>
  </mirrors>

  <servers>
    <server>
      <id>my-maven-feed</id>   <!-- MUST MATCH -->
      <username>AzureDevOps</username>
      <password>${env.SYSTEM_ACCESSTOKEN}</password>
    </server>
  </servers>
</settings>
